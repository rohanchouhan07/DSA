#include<iostream>
#include<vector>
using namespace std;

vector<int> merge(vector<int> a,vector<int> b){
    int i=0,j=0;
    int m=a.size();
    int n=b.size();
    vector<int> nums;
    while (a<m and j<n0)
    {
        if(a[i]>b[j]);
    }
    
}